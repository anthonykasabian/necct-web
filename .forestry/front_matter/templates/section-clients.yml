---
label: Section Clients
hide_body: true
fields:
- name: tagline
  type: text
  config:
    required: false
  label: Tagline
- name: section_title
  type: text
  config:
    required: false
  label: Section Title
- name: section_text
  type: textarea
  default: ''
  config:
    required: false
    wysiwyg: false
    schema:
      format: markdown
  label: Section text
- name: call_to_action
  type: select
  default: []
  config:
    required: false
    options: []
    source:
      type: pages
      section: 
      file: 
      path: 
  label: Call to action
- name: client_list
  type: field_group_list
  fields:
  - name: client_name
    type: text
    config:
      required: false
    label: Client Name
  - name: client_image
    type: file
    config:
      maxSize: 64
    label: Client Image
  config:
    min: 
    max: 
    labelField: 
  label: Client List
